/**
 * Write a description of class WheelStrut here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WheelStrut
{
    Wheel left;
    Wheel right;
    private boolean deployed;
    private char ID;
    
    /** 
     * Constructor for the WheelStrut class
     * 
     * @param The default ID value
     * @param The default wheelSize
     */    
    public WheelStrut(char ID, double wheelSize) throws Exception
    {
        //it creates two wheels with a size and max pressure
        left = new Wheel(wheelSize, wheelSize);
        right = new Wheel(wheelSize, wheelSize);
        
        setID(ID);
    }
    
    /**
     * It allows you to change the ID field
     * 
     * @param the new ID value
     */
    public void setID(char ID)
    {
        this.ID = ID;
    }
    
    /**
     * It returns the current ID of the wheelStrut
     * 
     * @return the ID of the wheelStrut
     */    
    public char getID()
    {
        return ID;
    }
    
    /**
     * It returns if the wheel strut works correctly
     * 
     * @return true if it's ok, false otherwise
     */
    public boolean isOperational()
    {
        return left.isOperational() && right.isOperational();
    }
    
    /**
     * It makes the wheelstrut being deployed or not
     * 
     * @param the new value for the deployed field
     */
    public void setDeployed(boolean deployed)
    {
        this.deployed = deployed;        
    }
    
    /**
     * It checks if the wheelstrut is deployed or not
     * 
     * @return true if it's deployed, false otherwise
     */
    public boolean isDeployed()
    {
        return deployed;           
    }
    
    /**
     * It returns the condition of the wheelstrut as a string
     * 
     * @return a string containing the values of the wheelstrut class
     */
    public String toString()
    {
        return "ID: " + getID() + " - Deployed: " 
        + isDeployed() + " - Op: " + isOperational() +
        " [L: " + left.isOperational() + "][R: " 
        + right.isOperational() + "]";
    }
    
    /**
     * It prints the details of the wheel and the wheelstrut
     */
    public void print()
    {
        System.out.println (toString());
        System.out.println ("L: " + left.toString());
        System.out.println ("R: " + right.toString());
    }
}
